export declare class Utilities {
    static assetUrl(url: string): string;
    static setCookie(name: string, val: string, exp: number): void;
    static getCookie(name: string): any;
    static deleteCookie(name: string): void;
}
